import { defineMessages } from 'react-intl'

export default defineMessages({
  userHint: {
    id: 'containers.HomePage.user.hint',
    defaultMessage: 'Type user name in the input above and press Enter',
  },
})
